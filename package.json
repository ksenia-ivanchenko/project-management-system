{
  "scripts": {
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "start": "npm run docker:up && wait-on http://localhost:5173 && open http://localhost:5173"
  }
}
